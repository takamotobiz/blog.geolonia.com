{
  "name": "blog.geolonia.com",
  "author": "Geolonia Inc.",
  "scripts": {
    "start": "bundle exec jekyll serve",
    "init": "bundle install --path vendor",
    "build": "npm run init && bundle exec jekyll build && cp _headers _site/"
  }
}
